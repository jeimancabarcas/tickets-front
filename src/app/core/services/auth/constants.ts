import { Injectable } from '@angular/core';
import { EMPTY_MESSAGE } from '../../constants/messages.constants';

@Injectable({
  providedIn: 'root'
})
export class Constants {
  EMPTY_MESSAGE = EMPTY_MESSAGE;
}
